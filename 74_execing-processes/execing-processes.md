#### JavaScript has no `syscall.Exec` equivalent to replace the current process; this lesson uses `node:child_process` `execFileSync` to run an external command synchronously and capture its output.
___
##### Run Command:

`$ deno run --allow-run --allow-env execing-processes.ts`

##### Results:

`deno 2.x.x`
