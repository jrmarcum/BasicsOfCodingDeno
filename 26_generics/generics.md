#### Go generics with type parameters map directly to TypeScript generics; JavaScript achieves the same with dynamic typing and no generic syntax.
___
##### Run Command:

`$ deno run generics.ts`

##### Results:

`index of zoo: 2`
`list: [ 10, 13, 23 ]`
