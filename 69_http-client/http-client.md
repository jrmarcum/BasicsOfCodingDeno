#### Go's `http.Get` maps to the global `fetch` built into Deno; this example requires internet access and the response body will vary.
___
##### Run Command:

`$ deno run --allow-net http-client.ts`

##### Results:

`Response status: 200`
`<!DOCTYPE html>`
`<html>`
`  <head>`
`    <meta charset="utf-8">`
`    <title>Go by Example</title>`
